# Review Fixes Plan — Shuffled Round Robin + Bumpers

Fixes for the verified findings of the 2026-07-10 max-effort code review. Every finding was CONFIRMED by an independent verifier with quoted lines. Line numbers are approximate — locate by anchor text.

Build gate for every task: `cd dev && ./build-local.sh` (net10.0) plus `dotnet build Jellyfin.Plugin.SmartLists --framework net9.0 --configuration Release` — warnings are errors. JS gate: `node --check` on every modified config-*.js. UI JS constraints: no ES6 template literals, never `is="emby-input"`, user messages via `showNotification()`.

## Task 1 — Backend bumper correctness (`PlaylistService.cs`, `InputValidator.cs`, `DtoMapper.cs`, `RoundRobinOrder.cs`)

1. **Bumper pool must include extras** (finding #1): in `GetBumperItemIds`, add `IncludeExtras = true,` to the synthetic `bumperDto` object initializer (~line 1052). Trailers/interstitials are the archetypal bumper content; bumper rules still filter. Add a code comment stating extras are always fetched for bumper pools.
2. **Validator refactor + gaps** (findings #6, #7, #13): in `InputValidator.ValidateSmartList`:
   - Extract private helpers `ValidateMediaTypeValues(List<string>? types, string label)` (the MediaTypes.All membership check with the full "Allowed types: ..." message) and use it for BOTH the main block (~274-285) and the bumper block — behavior of the main path unchanged.
   - Bumpers present (`playlistDto.Bumpers != null` with non-empty ExpressionSets) now REQUIRES non-empty `MediaTypes` — failure message: "Bumpers require at least one media type".
   - Bumper media types must additionally be playlist-supported: reject `Series`, `Season`, `MusicAlbum` (mirror `PlaylistService.ValidateUnsupportedMediaTypes` semantics) — message names the offending type and says bumpers are woven into playlists.
   - Replace the hand-rolled `if (bumpers.Interval < 1)` with `ValidateInteger(bumpers.Interval, min: 1, max: 10000, fieldName: "Bumper interval")` (match the call style of the existing MaxItems/MaxPlayTimeMinutes usages ~341/347).
   - Extract `ValidateRuleGroups(List<ExpressionSet>? sets, string label)` wrapping the count-cap + `ValidateExpressionSets` shape and call from both main and bumper paths.
3. **Defense in depth at the service** (finding #7): in `GetBumperItemIds`, before fetching media, call the existing `ValidateUnsupportedMediaTypes(bumpers.MediaTypes, dto.Name + " (Bumpers)")`-style guard used by the main path (~line 411/979); on failure log a warning and return `[]` — no throw.
4. **Dedupe the bumper pool** (finding #9): the id loop in `GetBumperItemIds` must skip ids already added — add a `seen` HashSet or apply `.Distinct()` to `bumperIds` before the loop. Commit e82ea0e added the main path's Distinct for exactly this (LibraryName virtual-item queries emit duplicates).
5. **Kill the GroupBy waste** (finding #12, cheap part only): replace `bumperMediaLookup` (GroupBy+ToDictionary over the full bumper scan, ~1070-1072) with: build `HashSet<Guid> bumperIdSet` from `bumperIds`, single pass over `bumperMedia` adding matched items into a small `Dictionary<Guid, BaseItem>`, then iterate `bumperIds` in order resolving from that dictionary (preserves order, drops ids absent from bumperMedia, combines naturally with the dedupe above). Do NOT attempt allUserMedia reuse (extras/library-rule scopes differ now that bumpers always include extras).
6. **Delete dead mapping line**: `DtoMapper.cs` ~51 `Bumpers = (source as SmartPlaylistDto)?.Bumpers,` — provably dead (early return at ~24); verified safe to delete (all callers pre-cast; JSON converter yields concrete DTOs).
7. **Deduplicate shuffle override**: `RoundRobinOrder.cs` — change `RoundRobinShuffledOrder` to derive from `RoundRobinRandomOrder`, overriding only `Name` and `ShuffleWithinGroups`; delete its copy of `OrderGroupKeys`. Verified behavior-identical (only construction site is the name-keyed OrderMap; no code type-checks RoundRobinRandomOrder).
8. **Log the silent round-robin no-op** (finding #10): in `BuildInterleavedPositions`, when `groupByField` is null/empty and the item list is non-empty, `logger?.LogWarning("{LogPrefix}: no GroupByField configured - items returned in original order", logPrefix);` before returning.

## Task 2 — Auto-refresh bumper awareness (`AutoRefreshService.cs`)

Finding #4 (CONFIRMED: zero 'Bumper' references in the file). In `AddPlaylistToRuleCache` (~508): union `playlist.Bumpers?.MediaTypes` into the media-type list used for `_mediaTypeToPlaylistsCache`, and union `playlist.Bumpers?.ExpressionSets` into the sets analyzed for `_ruleTypeToPlaylistsCache` (and any user-specific-rule scan ~1105). In the fallback `IsItemRelevantToPlaylist` (~1244-1252): also return true when `playlist.Bumpers?.MediaTypes?.Contains(itemMediaType) == true`. Cast note: the cache builder works on the playlist DTO — confirm it is `SmartPlaylistDto` (or pattern-match `as SmartPlaylistDto`); collections have no Bumpers. Keep changes surgical — same style as existing code.

## Task 3 — Save-path integrity + scope guards (config JS, both HTML pages)

1. **Populate bumper media-type select from the constant** (findings #3, #15): remove the hardcoded `<option>`s (keep only `<option value="">None (disabled)</option>`) from `#bumperMediaType` in BOTH `config.html` and `user-playlists.html`. In `config-init.js` (where the bumper listeners are wired), populate the select via `SmartLists.populateSelectElement` from the canonical media-type constant in `config-core.js` (~127-140), filtered to playlist-supported playable types: exclude `Series`, `Season`, `MusicAlbum` (matches Task 1's validator). Prepend the None option. Use the constant's exact key casing (Value/Label).
2. **No silent wipe of unknown saved types** (finding #3): in `populateBumperConfigIntoForm`, after assigning `bumperMediaSelect.value`, if the assignment did not stick (`bumperMediaSelect.value !== savedType`) call `SmartLists.showNotification('Bumper media type "' + savedType + '" is not supported for playlists - bumper configuration disabled.')` — the visible warning replaces today's silent reset.
3. **Block save on incomplete bumper rules** (finding #2): `collectBumperConfigFromForm` currently returns null when media type is set but no complete rules exist. Change contract: return the string `'invalid'` in that specific case (media type non-empty AND zero complete expression sets), null only when media type is empty (feature off). In the save handler in `config-lists.js` (~391), check for `'invalid'`: `showNotification('Bumper rules are incomplete. Complete at least one bumper rule or set the bumper media type to None.')` and abort the save (return before the API call), mirroring how other validation failures abort.
4. **Scope-guard autoCheckIncludeExtras** (finding #5): at both call sites (config-rules.js ~1351 and ~1967), only call when the row is main-scoped: `if (newField === 'ExtraType' && SmartLists.getRowScope(ruleRow) === 'main') { SmartLists.autoCheckIncludeExtras(page); }` (adjust variable names to local context).
5. **Fix the listener leak + deduplicate reset** (finding #8 + cut item): inside `populateBumperConfigIntoForm`, before `bumperRulesContainer.innerHTML = ''`, run `bumperRulesContainer.querySelectorAll('.rule-row').forEach(function (rule) { SmartLists.cleanupRuleEventListeners(rule); });`. Then replace the clearForm bumper-reset block (config-lists.js ~662-680) with a single call `SmartLists.populateBumperConfigIntoForm(page, {});` (verified behavior-identical: else-branch reproduces the reset trio + visibility update, and cleanup now lives inside).
6. **Cheap perf fixes** (cut items): (a) remove the duplicate `updateLogicGroupMoveButtonVisibility(page)` call at the end of `removeLogicGroup` (~1815) — `updateRuleButtonVisibility`'s tail (~1881) already runs it; (b) in `updateAllFieldSelects`, hoist the two possible media-type arrays before the row loop (`var mainTypes = SmartLists.getSelectedMediaTypes(page, 'main'); var bumperTypes = SmartLists.getSelectedMediaTypes(page, 'bumper');`) and pick per row by `getRowScope` — restoring the pre-diff hoist the feature removed. Do NOT refactor populateFieldSelect's internals or the five visibility helpers.

## Task 4 — Centralize sort-name predicates (config JS + `SmartList.cs`)

Finding #14 (CONFIRMED: exactly 7 JS chains + 1 backend enumeration; refactor verified behavior-safe).
1. In `config-core.js`, next to `SORT_OPTIONS`: `SmartLists.ORDERLESS_SORTS = ['Random', 'Random Round Robin', 'Shuffled Round Robin', 'NoOrder'];` and `SmartLists.ROUND_ROBIN_SORTS = ['Round Robin', 'Random Round Robin', 'Shuffled Round Robin'];` plus helpers `SmartLists.isOrderlessSort(name)` / `SmartLists.isRoundRobinSort(name)` using `indexOf !== -1`. Note: the display-formatting chain (config-formatters.js ~438) also accepts legacy `'No Order'`/`'Default'` — keep those extra checks inline there alongside the predicate.
2. Replace the chains at config-sorts.js ~122, ~135, ~293, ~428, ~441, ~580 and config-formatters.js ~438 with the predicates. Semantics per site are already split orderless-vs-roundrobin — match each site to the right predicate (sites 122/428/580/formatters = orderless; 135/293/441 = round robin).
3. C# (`SmartList.cs` `InitializeFromDto` ~117-140): replace the two special-case branches (`"Random" || "NoOrder"` and `"Random Round Robin" || "Shuffled Round Robin"`) with a single lookup: if `OrderFactory.OrderMap` contains the bare `so.SortBy` (add a small `OrderFactory.IsDirectionless(string name)` static backed by `OrderMap.ContainsKey`), call `OrderFactory.CreateOrder(so.SortBy)` and set `GroupByField` when the result `is RoundRobinBase` (the generic `is RoundRobinBase` assignment already exists below — merge so it applies to both paths, preserving current behavior exactly).

## Task 5 — Docs + verification

1. `docs/content/user-guide/bumpers.md`: document the new behaviors — bumper media types limited to playlist-supported types; bumper pools always include extras (trailers etc.); incomplete bumper rules now block saving with a message.
2. Full regression: build both targets; via REST API (dev key) create a playlist with Shuffled Round Robin + bumpers (`ExtraType`-based rule if the dev library has trailers, else Name-based), refresh twice, verify weave log line + wraparound + reshuffle; verify a Bumpers config with `MediaTypes: []` is now rejected at save; verify existing playlists still refresh unchanged. Delete test playlists afterward.
